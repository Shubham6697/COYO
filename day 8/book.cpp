#include<bits/stdc++.h>
using namespace std;



int main()
{
    struct book
    {
        int page;
        float price;
    };
    struct book a,b;
    cout<<"no. of page in book a ";
    cin>>a.page;
    cout<<"price of book a ";
    cin>>a.price;
    cout<<"no. of page in book b ";
    cin>>b.page;
    cout<<"price of book a ";
    cin>>b.price;

    return 0;
}
